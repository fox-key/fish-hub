{
  "name": "helloNav",
  "version": "0.1.1",
  "description": "blog",
  "main": "src/index.js",
  "license": "ISC",
  "homepage": ".",
  "private": true,
  "scripts": {
    "start": "craco start",
    "build": "npm version patch && craco build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
