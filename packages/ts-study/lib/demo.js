const foo = true;
const foo1 = true;
function process1(input) {
    input === null || input === void 0 ? void 0 : input.handler();
}
function ensureArray(input) {
    return Array.isArray(input) ? input : [input];
}
const profile = {
    name: 'erer',
    age: 18
};
const str = 'fengkai';
const obj = {
    name: 'fengkai'
};
const nullVar = null;
const undefinedVar = undefined;
const func = (input) => input.length > 10;
const fn1 = (input) => {
    return input.length > 10;
};
const fn2 = (name) => {
    return name === 'fengkai';
};
function foo3(input) {
    if (type)
        ;
}
